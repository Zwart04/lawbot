/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
  // remove remotePatterns that don't work with static export
};

module.exports = nextConfig;
